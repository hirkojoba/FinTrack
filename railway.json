{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bundle install && apt-get update && apt-get install -y python3 python3-pip && pip3 install -r ml_service/requirements.txt"
  },
  "deploy": {
    "startCommand": "bundle exec rails server -e production",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
